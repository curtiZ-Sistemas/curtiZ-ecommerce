Analise completamente o projeto curti Z antes de fazer qualquer alteração.

O domínio oficial de produção será:

https://curtiz.com.br

O endereço atual `*.workers.dev` da aplicação deve CONTINUAR funcionando como ambiente/endereço de teste.

OBJETIVO PRINCIPAL

Preparar o projeto para receber `https://curtiz.com.br` na Cloudflare sem quebrar absolutamente nenhuma funcionalidade existente e sem deixar o projeto permanentemente dependente desse domínio.

Quero poder:

* usar `https://curtiz.com.br` em produção;
* continuar acessando a aplicação pelo endereço atual `workers.dev` para testes;
* temporariamente retirar o domínio oficial se necessário;
* posteriormente colocar `curtiz.com.br` novamente sem precisar alterar código;
* alternar URLs através de configuração/variáveis de ambiente;
* manter autenticação, Supabase, APIs, imagens, cookies, redirects e painel funcionando nos dois ambientes.

ANTES DE ALTERAR

Faça uma auditoria em todo o monorepo procurando:

* URLs `workers.dev` hardcoded;
* localhost hardcoded;
* URLs absolutas hardcoded;
* URLs de callback;
* URLs de autenticação;
* redirectTo;
* Site URL;
* CORS;
* allowed origins;
* cookies;
* domínio configurado em cookies;
* CSP;
* headers;
* middleware;
* APIs internas;
* chamadas fetch;
* webhooks;
* páginas de login;
* cadastro;
* recuperação de senha;
* confirmação de e-mail;
* login social;
* Supabase Auth;
* metadata;
* OpenGraph;
* canonical URLs;
* sitemap;
* robots.txt;
* manifest;
* compartilhamento de produtos;
* links absolutos enviados por e-mail;
* checkout;
* carrinho;
* painel administrativo;
* painel cliente;
* painel representante;
* painel operacional;
* painel gerencial.

NÃO substitua cegamente todas as URLs.

ARQUITETURA

Quero que exista uma única fonte confiável para determinar a URL pública da aplicação.

Primeiro verifique se o projeto já possui alguma variável como:

APP_URL
SITE_URL
PUBLIC_SITE_URL
NEXT_PUBLIC_SITE_URL
NEXT_PUBLIC_APP_URL

Se já existir uma arquitetura correta, reutilize-a.

Não crie variáveis duplicadas desnecessariamente.

Se não existir, implemente uma solução simples e centralizada apropriada à arquitetura atual do projeto.

Produção:

https://curtiz.com.br

Teste atual:

identifique automaticamente no projeto/configuração o endereço `workers.dev` que já está sendo utilizado.

Nunca coloque secrets diretamente no código.

Não coloque informações sensíveis em variáveis públicas.

URLS ABSOLUTAS

Todos os locais que realmente precisarem conhecer a URL pública devem obtê-la da configuração adequada.

Quando for tecnicamente melhor utilizar a origem da própria requisição (`request.url`, `origin`, headers confiáveis etc.), prefira essa abordagem para permitir que tanto `curtiz.com.br` quanto `workers.dev` funcionem sem alteração manual.

Evite gerar URLs absolutas desnecessariamente.

AUTH / SUPABASE

Analise toda a implementação atual do Supabase Auth.

Garanta funcionamento em:

https://curtiz.com.br

e no domínio atual:

https://*.workers.dev

Verifique especificamente:

* login;
* cadastro;
* logout;
* confirmação de e-mail;
* recuperação de senha;
* redefinição de senha;
* magic links, caso existam;
* OAuth, caso exista;
* redirectTo;
* callbacks;
* refresh de sessão;
* SSR;
* middleware;
* cookies.

Não altere chaves, banco, RLS ou estrutura de usuários sem necessidade.

Ao final, informe exatamente quais URLs eu preciso cadastrar em:

Supabase > Authentication > URL Configuration > Redirect URLs

Não invente rotas.

Analise o projeto e apresente as rotas REAIS utilizadas.

COOKIES

Garanta que cookies de sessão não fiquem hardcoded para um hostname antigo.

Não configure um cookie que impeça o funcionamento no `workers.dev`.

Mantenha:

Secure em produção;
HttpOnly quando aplicável;
SameSite adequado ao fluxo existente.

Não reduza a segurança para permitir os dois domínios.

CORS

Não utilize `Access-Control-Allow-Origin: *` em endpoints autenticados ou que utilizem credenciais.

Crie allowlist somente se a arquitetura realmente necessitar de CORS.

Se necessário, permita exatamente os hosts legítimos de produção e teste.

WWW

O endereço canônico será:

https://curtiz.com.br

`https://www.curtiz.com.br` deverá redirecionar permanentemente para:

https://curtiz.com.br

Não crie redirecionamento de `workers.dev` para `curtiz.com.br`, pois quero continuar usando `workers.dev` como endereço independente de teste.

SEO

Atualize de forma configurável, onde necessário:

* canonical;
* metadataBase;
* sitemap;
* robots;
* OpenGraph;
* compartilhamento;
* URLs estruturadas.

Em produção, mecanismos de busca devem considerar:

https://curtiz.com.br

como domínio canônico.

Evite indexação indevida do ambiente `workers.dev`, mas NÃO bloqueie seu uso normal para testes.

CLOUDFLARE

Analise:

* wrangler.toml;
* wrangler.json;
* wrangler.jsonc;
* configuração Workers;
* scripts de deploy;
* GitHub Actions;
* configurações de produção;
* configurações de preview.

Não adicione o domínio diretamente ao Wrangler se isso puder causar acoplamento desnecessário ou interferir na configuração de Custom Domain feita pelo painel da Cloudflare.

Se houver benefício real em configurar routes/custom_domain no Wrangler, explique antes no relatório e implemente somente se for seguro para a arquitetura existente.

Não desative `workers.dev`.

DEPLOY

Não altere o fluxo GitHub → Cloudflare já existente de maneira que possa interromper os deploys automáticos.

Preserve:

* builds atuais;
* Worker atual;
* bindings;
* secrets;
* Supabase;
* banco;
* cache;
* assets;
* APIs;
* rotas.

INTEGRAÇÕES FUTURAS

Ainda serão configurados, nesta ordem:

1. Resend
2. Mercado Pago
3. Melhor Envio
4. Bling

Não implemente essas integrações agora.

Apenas deixe a arquitetura de URLs preparada para que futuramente callbacks/webhooks possam utilizar:

https://curtiz.com.br

sem URLs hardcoded espalhadas pelo código.

TESTES OBRIGATÓRIOS

Depois das alterações execute tudo que o projeto disponibilizar, incluindo quando aplicável:

* typecheck;
* lint;
* build;
* testes unitários;
* testes de integração;
* testes E2E.

Garanta que nenhuma alteração introduziu regressões.

Faça também busca final no repositório por:

workers.dev
localhost
http://
https://

e analise individualmente os resultados para garantir que não sobraram URLs hardcoded problemáticas.

Não remova URLs legítimas de documentação, testes ou configuração local sem necessidade.

ENTREGA

Ao terminar, quero um relatório curto contendo:

1. arquivos alterados;
2. motivo de cada alteração;
3. variável ou mecanismo definitivo usado para a URL da aplicação;
4. variável que devo configurar na Cloudflare;
5. valor de PRODUÇÃO dessa variável;
6. valor de TESTE dessa variável;
7. URLs exatas que devo cadastrar no Supabase Redirect URLs;
8. se preciso alterar alguma configuração manual na Cloudflare;
9. se preciso alterar alguma configuração no GitHub;
10. confirmação de que `workers.dev` continua funcionando;
11. confirmação de que `curtiz.com.br` pode ser removido e recolocado sem alterações no código;
12. resultado de build, lint, typecheck e testes.

IMPORTANTE

Não faça refatorações grandes fora desse escopo.

Não redesenhe nenhuma página.

Não altere banco ou schema sem necessidade comprovada.

Não remova funcionalidades existentes.

Não desative o endereço de teste `workers.dev`.

Faça primeiro a auditoria, implemente somente o necessário e deixe o sistema preparado de forma profissional para produção.

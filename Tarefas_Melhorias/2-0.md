Corrija a tela de login da loja **curti Z**.

Existe atualmente uma **faixa cinza horizontal desnecessária entre a área principal do login e o footer**, visível no desktop.

Quero remover completamente esse espaço/faixa sem criar gambiarra.

### Resultado esperado

A estrutura deve ficar visualmente contínua:

```text
HEADER

ÁREA DE LOGIN
[ card de login ]

FOOTER
```

Não quero:

```text
HEADER

ÁREA DE LOGIN

FAIXA CINZA VAZIA

FOOTER
```

### Investigue a causa real

Verifique principalmente:

* `min-height`;
* `height`;
* `padding-bottom`;
* `margin-bottom`;
* wrappers;
* background do `body`;
* background do `main`;
* background da página de autenticação;
* containers entre o login e o footer;
* `#eeeeee` ou outro background estrutural herdado;
* flex/grid que esteja criando espaço vazio;
* regras responsivas.

Não esconda a faixa com `overflow: hidden` ou posicionamento absoluto.

Corrija a estrutura/CSS responsável.

### Aparência

A área do login deve manter o visual atual.

Não redesenhe:

* card;
* formulário;
* header;
* footer;
* identidade visual.

Apenas ajuste o layout para que a transição entre login e footer fique limpa e profissional.

A área ao redor do card pode continuar clara/branca conforme o design atual.

O footer deve começar naturalmente logo após a seção do login, respeitando um espaçamento intencional e moderado, sem uma faixa cinza vazia.

### Altura da página

Em telas grandes, se o conteúdo não preencher o viewport:

* use uma estrutura correta com `min-height`;
* header + main + footer devem compor a tela naturalmente;
* `main` pode crescer para ocupar o espaço disponível;
* não criar uma região de background diferente apenas para preencher altura.

Prefira uma estrutura semelhante a:

```css
.page {
  min-height: 100dvh;
  display: flex;
  flex-direction: column;
}

main {
  flex: 1;
}
```

Somente aplique essa abordagem se for compatível com a arquitetura existente.

### Responsividade

Valide em:

* desktop;
* notebook;
* tablet;
* mobile.

Garanta:

* nenhuma faixa cinza inesperada;
* nenhuma sobreposição;
* footer correto;
* login centralizado;
* sem scroll vertical artificial;
* sem conteúdo cortado.

Faça a correção diretamente no código e informe no final apenas:

1. causa encontrada;
2. arquivo(s) alterado(s);
3. como foi corrigido;
4. validação desktop/mobile.
